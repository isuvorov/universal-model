import UniversalModel from '../../../XyuUniversalModel'; // eslint-disable-line
import UniversalPet from '../../../UniversalPet'; // eslint-disable-line
export default (ctx) => {
  const { e403, e400, e404, e500 } = ctx.errors // eslint-disable-line
  const { isAuth, _checkNotFound } = ctx.helpers; // eslint-disable-line

  const controller = {};
  
  return controller;
};
